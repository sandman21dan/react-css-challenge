import * as React from 'react';

import './CloseButton.css';

const CloseButton: React.StatelessComponent = () => (
  <span className="close-btn" role="button">X</span>
);

export default CloseButton;
