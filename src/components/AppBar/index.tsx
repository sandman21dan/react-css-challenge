import * as React from 'react';

import './AppBar.css';

const AppBar: React.StatelessComponent = () => (
  <div className="app-bar">
    <h1 className="app-bar__title">Demo app</h1>
  </div>
);

export default AppBar;
